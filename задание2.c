include <stdio.h> 
#include <locale.h>
#include <math.h> 
#define N 10
#define K 100
int main() {
	int A[K];
	int temp;
	int i,x;
	float z,k1,k2,xx;
	setlocale(0, "rus");
	puts("Задна функция у = 1.8x^2 - sin10x и интервал[0.2;2.2]\n");
		    scanf("%d", &temp);
			for (int i = ; i < K; i++) {
				A[i] = 1.8 * (float)pow(x, 2) - (float)sin(10 * x);
				if (A[(int)x] >= 0) {
					z += A[(int)x];
					k1
				}
			}

			

	
	

}
